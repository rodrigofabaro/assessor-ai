export default function Home() {
  return (
    <main style={{ padding: 8 }}>
      <h1>Assessor AI</h1>
      <p>
        Phase 1 is live (uploads + submissions). Phase 2 adds a reference library
        for specs/briefs and structured criteria.
      </p>
      <ul>
        <li>Go to <b>Admin: Reference</b> to upload specs/briefs and build criteria.</li>
        <li>Go to <b>Upload</b> to upload student submissions (Phase 1).</li>
      </ul>
    </main>
  );
}
